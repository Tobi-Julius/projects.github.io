import NavLinks from "./NavLinks"
const Desktop = () => {
    return (
        <nav className="Desktop-nav">
            <NavLinks className="desk-link"/>
        </nav>
    )
}

export default Desktop